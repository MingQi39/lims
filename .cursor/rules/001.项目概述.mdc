---
description: LIMS 系统定位与技术选型
alwaysApply: true
---

# LIMS 项目概述

实验室信息管理系统，覆盖样品管理、实验流程、报告生成（Excel 模版→PDF 证书）、审批流转。核心产出物为校准/检测证书 PDF（单份可达 100 页），系统需支撑千级文件的高速编辑、生成与查看。

## 设计原则

请求可追踪（trace_id 全链路）| 写操作可回溯（审计日志）| 业务一致性（事务 + Outbox）| 异常可定位（结构化日志 + Sentry）| 耗时异步化（Asynq Worker）| 接口幂等 | 安全纵深 | 分层解耦（DDD）| 可水平扩展（K8s HPA）| 可部署可运维（Helm + GitOps）

## 部署模式

系统需同时支持**云端部署**与**客户本地私有化部署**两种模式，共用同一套代码。

| 模式       | 场景                     | 数据库                | 入口              |
| ---------- | ------------------------ | --------------------- | ----------------- |
| 云端 SaaS  | 自有 ECS / K8s 集群      | 托管 RDS / Supabase   | 公网域名 + HTTPS  |
| 客户私有化 | 客户自购服务器，内网部署 | 容器内自带 PostgreSQL | IP 或内网域名访问 |

## 私有化部署约束（编码时必须遵守）

- **禁止硬编码外部服务地址**：所有外部连接（DB、Redis、第三方 API）必须通过环境变量配置
- **禁止依赖公网可达**：前端不得引用外部 CDN（字体、JS、CSS）；后端不得在启动或核心流程中调用公网 API
- **配置驱动**：功能开关、服务地址、密钥等全部通过环境变量注入，不使用配置文件
- **CORS 支持通配符**：`CORS_ALLOWED_ORIGINS=*` 时允许所有来源，适配 IP 访问场景
- **离线可构建**：Docker 镜像构建完成后，可通过 `docker save/load` 实现无网络环境部署
- **数据自包含**：私有化模式下 PostgreSQL、Redis 均运行在容器内，数据通过 Docker Volume 持久化

## 后端技术栈

Go >= 1.22 | Gin >= 1.9 | GORM >= 2.0 | PostgreSQL >= 15（云端：Supabase / 阿里云 RDS；私有化：容器内自带；HA：流复制 + repmgr）| Redis >= 7.0 | Asynq >= 0.24 | Zap 日志 | OpenTelemetry | Prometheus + Grafana | Sentry | excelize（Excel 读写）| LibreOffice headless（xlsx→PDF，100 页 5~15s）

## 前端技术栈

React >= 18 | TypeScript >= 5.0 | React Router v6 | Zustand | TanStack Query v5 | Ant Design >= 5.0 | Axios >= 1.6 | Vite | ESLint + Prettier | Vitest | Univer + ExcelJS（Excel 在线编辑）| PDF.js + react-pdf（PDF 查看，分页懒加载）

## 基础设施

Kubernetes >= 1.28 | Helm >= 3 | Docker Compose（私有化单机）| Loki / ELK | GitLab CI | MinIO（私有化可选文件存储）| PgBouncer（连接池）

## 文件处理性能约束（编码时必须遵守）

- **PDF 生成主引擎为 LibreOffice headless**：原生 xlsx→pdf 转换，速度 20x 于 chromedp，禁止用 chromedp 作为默认引擎
- **Worker 并行处理**：证书 PDF 生成等重操作必须走 Asynq Worker 异步处理，禁止在 API Server 中同步执行
- **Excel 模版变量替换用 excelize**：仅扫描非空单元格中的 `{{变量名}}` 占位符，禁止全表遍历
- **PDF 查看用 PDF.js 分页懒加载**：首页 < 1s，禁止整份 PDF 下载后再渲染
- **业务列表缓存**：客户、标准方法等变更低频的查询结果走 Redis 缓存（TTL 5~10min），写操作后失效
- **读写分离已就绪**：`DB_REPLICA_DSNS` + GORM dbresolver，列表/搜索类查询自动走只读副本
