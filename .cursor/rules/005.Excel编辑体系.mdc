---
description: Electron 与 Web 端 Excel 模版编辑的完整架构，涉及修改 Excel 编辑、Univer、ExcelJS、模版管理、IPC 通信、sheet 裁剪/合并等场景时参考
globs:
  - lims-electron/src/hooks/useNativeExcelEdit.ts
  - lims-electron/src/components/ExcelEditorModal/**
  - lims-electron/src/utils/excelSheetStrip.ts
  - lims-electron/src/pages/template/**/useNativeExcelEdit.ts
  - lims-electron/electron/main.ts
  - lims-electron/electron/preload.ts
alwaysApply: false
---

# Excel 编辑体系

系统存在**两条互斥的 Excel 编辑路径**，根据运行环境（`isElectron()` / `window.electronAPI`）自动切换。

## 一、双路径总览

| 维度 | Electron 桌面端 | Web 浏览器端 |
|------|----------------|-------------|
| 编辑器 | 系统原生 Excel（WPS / Microsoft Excel） | 内嵌 Univer 电子表格引擎 |
| 入口 Hook | `useNativeExcelEdit`（`@/hooks/`） | `useExcelEditor`（`ExcelEditorModal/`） |
| UI 形态 | 调起外部应用，无弹窗 | Ant Design Modal 内嵌 Univer 画布 |
| 保存机制 | `fs.watch` + 轮询检测文件变化 → 自动上传 | 手动点击「保存」→ ExcelJS 写回 → 上传 |
| 格式保真度 | 极高（原生 Excel 打开原始 xlsx） | 有限（ExcelJS→Univer 转换存在样式损失） |

## 二、Electron 端：系统 Excel 外部编辑

### 2.1 核心流程

```
getFileBlob(fileId)
  → removeExternalLinks(buffer)          // 去掉外部链接警告
  → stripExcelSheets(buffer, sheets)     // 按需裁剪不相关 sheet
  → electronAPI.openFileInSystemApp()    // IPC → 主进程写临时文件 + shell.openPath
  → electronAPI.watchFileChanges()       // 主进程 fs.watch + 3s 轮询
  → [用户在 Excel 中编辑并保存]
  → onFileChanged 回调                   // 主进程 hash 对比后推送
  → mergeEditedSheets()                  // 把编辑后 sheet 合并回原始完整文件
  → onFileSync()                         // 上传到服务器 API
```

### 2.2 IPC 通道（preload.ts ↔ main.ts）

| preload 暴露方法 | ipcMain handle | 说明 |
|-----------------|----------------|------|
| `openFileInSystemApp(buffer, name, readOnly?)` | `save-and-open-temp-file` | 写临时目录 `{temp}/lims-excel-edit/` + `shell.openPath`；只读时 `chmod 0o444` |
| `watchFileChanges(path)` | `watch-file-changes` | `fs.watch` 目录级监听 + `setInterval` 3s 轮询双保险；debounce 800ms；2s grace period |
| `stopWatchingFile(path)` | `stop-watching-file` | 停止 watcher + 删除临时文件 |
| `focusTempFile(path)` | `focus-temp-file` | 重复编辑同一模版时聚焦已打开的文件，避免重复下载 |
| — | `file-changed` 事件 | 主进程检测到 hash 变化后 `webContents.send` 推给渲染进程 |

### 2.3 安全措施

- 临时文件路径前缀校验（`realpath` + `startsWith`），防止路径穿越
- 所有文件操作限制在 `{temp}/lims-excel-edit/` 目录内
- 窗口关闭时 `stopAllFileWatchers()` 清理

### 2.4 Sheet 裁剪与合并（JSZip 层面）

**裁剪**（打开前）：`stripExcelSheets()` 在 ZIP 层面删除指定 sheet XML + rels + calcChain + Content_Types 条目。

**合并**（保存后）：`mergeEditedSheets()` 按 sheet 名称把编辑后 ZIP 中的 worksheet XML 直接复制回原始完整 ZIP。不解析单元格，速度远快于 ExcelJS 逐格合并。

业务预设常量：
- `CERT_COVER_SHEETS_TO_REMOVE = ["校准结果", "原始记录"]` — 证书封面模版编辑时裁掉
- `ORIGINAL_RECORD_SHEETS_TO_REMOVE = ["封面", "校准说明"]` — 原始记录模版编辑时裁掉

## 三、Web 端：Univer 内嵌编辑

### 3.1 核心流程

```
getFileBlob(fileId)
  → ExcelJS.Workbook.xlsx.load()         // 解析 xlsx
  → excelJSToUniverData()                // 转换为 Univer 工作簿数据结构
  → supplementMissingImages()            // Drawing XML 补充 ExcelJS 遗漏的图片
  → createUniver() + createWorkbook()    // 渲染到 Modal 内 div
  → [用户在 Univer 中编辑]
  → handleSave()                         // 读取 Univer 单元格值
  → ExcelJS 加载原始 blob → 逐格写回     // 保留原始样式/图片/格式
  → writeBuffer → File → onSave()        // 上传到服务器
```

### 3.2 关键转换

- **excelJSToUniverData**：遍历 worksheet 的行列数据、合并单元格、边框、样式，转为 Univer `IWorkbookData`
- **excelImageExtractor**：两层提取策略
  - 基础层：ExcelJS `getImages()` API
  - 补充层：直接解析 Drawing XML（覆盖 grpSp、AlternateContent/Fallback、EMF/WMF 矢量图的光栅替代）
- **保存回写策略**：加载**原始 blob** 到 ExcelJS → 只覆盖 Univer 中有值的单元格 → 跳过合并单元格的从属格 → 保留原始文件的样式/图片/格式不丢失

### 3.3 只读模式

传入 `readOnly=true` 时：隐藏工具栏和右键菜单、通过 `WorkbookPermissionPoint.Edit` 禁止编辑、隐藏保存按钮。

## 四、业务层封装

### 4.1 证书封面模版（`pages/template/certCover/`）

- `useNativeExcelEdit.ts`：包装基础 Hook，注入 `certCoverTemplateApi.update` 作为 `onFileSync`、`CERT_COVER_SHEETS_TO_REMOVE` 作为裁剪列表
- `List.tsx`：`IS_ELECTRON` ? 调 `nativeEdit.handleNativeEdit` : 弹 `ExcelEditorModal`

### 4.2 原始记录模版（`pages/template/originalRecord/`）

- `useNativeExcelEdit.ts`：包装基础 Hook，注入 `originalRecordTemplateApi.uploadExcel` 作为 `onFileSync`、`ORIGINAL_RECORD_SHEETS_TO_REMOVE`
- `List.tsx` / `OriginalRecordDetailPanel.tsx`：Electron 下只走原生编辑/查看，不展示 Univer 弹窗

### 4.3 封装层次

```
页面业务 Hook (certCover/useNativeExcelEdit.ts)
  └── 通用基础 Hook (@/hooks/useNativeExcelEdit.ts)
        ├── excelSheetStrip.ts (ZIP 层 sheet 裁剪/外部链接移除)
        ├── electronAPI (IPC 通信)
        └── mergeEditedSheets() (ZIP 层 sheet 合并)
```

## 五、外部链接移除

`removeExternalLinks()` 在 ZIP 层面清理 xlsx 中的 `<externalReferences>` 节点、相关 Relationship 和物理文件，避免系统 Excel 弹出"此工作簿包含到一个或多个可能不安全的外部源的链接"警告。

## 六、文件目录

| 文件 | 职责 |
|------|------|
| `hooks/useNativeExcelEdit.ts` | 通用 Electron 外部编辑 Hook（下载、打开、监听、合并、上传） |
| `components/ExcelEditorModal/useExcelEditor.ts` | Web 端 Univer 编辑 Hook |
| `components/ExcelEditorModal/ExcelEditorModal.tsx` | Univer 编辑器 Modal UI |
| `components/ExcelEditorModal/excelToUniverData.ts` | ExcelJS → Univer 数据转换 |
| `components/ExcelEditorModal/excelStyleConverter.ts` | 单元格样式转换 |
| `components/ExcelEditorModal/excelImageExtractor.ts` | 图片提取（ExcelJS API + Drawing XML 解析） |
| `components/ExcelEditorModal/types.ts` | 类型定义（ExtractedImage、Modal Props 等） |
| `utils/excelSheetStrip.ts` | ZIP 层 sheet 裁剪 + 外部链接移除 |
| `utils/platform.ts` | `isElectron()`（运行时检测 `window.electronAPI`） |
| `electron/main.ts` | 主进程：临时文件管理 + fs.watch + IPC handler |
| `electron/preload.ts` | contextBridge 暴露 `electronAPI` |
| `pages/template/certCover/useNativeExcelEdit.ts` | 证书封面业务封装 |
| `pages/template/originalRecord/useNativeExcelEdit.ts` | 原始记录业务封装 |

## 七、修改注意事项

- 共享前端代码（hooks/components/utils/pages）修改在 `lims-electron` 仓库根目录；Electron 主进程（main.ts/preload.ts）在 `lims-electron/electron/`
- `{{变量名}}` 模版替换在**后端** `tplengine` 中完成，前端不处理
- Excel → PDF 转换在**后端** LibreOffice headless 完成，前端不参与
- 所有 ZIP 层操作（JSZip）避免经过 ExcelJS 加载-重写，以保留原始文件格式
