---
description: Docker、K8s、私有化部署与 CI/CD 规范
globs: ["deploy/**", "Dockerfile*", ".gitlab-ci*"]
---

# 部署与运维规范

## Docker

- 后端镜像在 `lims-server` 构建和发布；`lims-electron` 通过 `LIMS_BACKEND_IMAGE` 引用，不检出后端源码
- 前端镜像在 `lims-electron` 根目录 `Dockerfile` 构建，运行时为 Nginx
- 后端多阶段构建（builder + alpine runtime）
- 后端非 root 用户运行，必须 HEALTHCHECK
- 后端构建使用 `CGO_ENABLED=0 GOOS=linux -ldflags="-s -w"`
- 日志驱动配置 `json-file` + `max-size` + `max-file`，防止磁盘写满
- Worker 镜像（如单独构建）由 `lims-server`/部署流水线负责，需包含 LibreOffice 与中文字体

## 部署架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    三种部署模式                           │
├──────────────────┬──────────────────┬────────────────────┤
│ 云端 K8s         │ 云端单机 ECS     │ 客户私有化          │
│ deploy/helm/     │ docker-compose   │ docker-compose     │
│                  │ .prod.yml        │ .onprem.yml        │
├──────────────────┼──────────────────┼────────────────────┤
│ 外部 RDS + Redis │ 外部 DB +容器Redis│ 全部容器内自带       │
│ Ingress + TLS    │ Caddy 自动 HTTPS │ Caddy (IP/域名)    │
│ Helm Chart       │ docker compose  │ install.sh/compose │
└──────────────────┴──────────────────┴────────────────────┘
```

## K8s 部署（云端）

| 项 | Backend | Worker |
|----|---------|--------|
| replicas | 3 | 2 |
| 资源请求 | 512Mi/500m | 512Mi/500m |
| 资源限制 | 1Gi/1000m | 2Gi/2000m |
| gracePeriod | 45s | 300s |

- 探针：`/healthz`(liveness) `/ready`(readiness+检查DB/Redis) `/healthz`(startup)
- 滚动更新 maxSurge=1 maxUnavailable=0，preStop sleep 5
- 安全上下文 runAsNonRoot + runAsUser=1000
- HPA：min=3 max=10，CPU 70% / Memory 80%
- PDB：minAvailable=2

## 客户私有化部署

### 架构

Caddy → frontend Nginx（`/api` 反代 → backend）→ PostgreSQL + Redis 全部容器内

### 文件结构

| 文件 | 用途 |
|------|------|
| `deploy/docker/docker-compose.onprem.yml` | 私有化编排（含 PG + Redis + 可选 MinIO） |
| `deploy/docker/docker-compose.onprem-ha.yml` | 私有化高可用编排（PG 流复制 + repmgr + PgBouncer + 读写分离） |
| `deploy/docker/Caddyfile.onprem` | 支持 IP / 内网域名 / 公网域名 |
| `deploy/docker/.env.onprem.example` | 配置模板（含 Worker / MinIO / 读写分离） |
| 后端镜像 `LIMS_BACKEND_IMAGE` | API Server / Worker / 迁移命令来自 `lims-server` 镜像 |
| `deploy/docker/install.sh` | 客户服务器一键安装脚本 |
| `docs/deploy-onprem.md` | 部署手册 |
| `docs/database-ha-performance.md` | 数据库 HA + 查询性能 + 文件处理性能方案 |

### 编码约束（强制）

- **禁止硬编码外部地址**：所有服务连接必须通过环境变量；不得写死 `supabase.co`、`amazonaws.com` 等地址
- **禁止外部 CDN 资源**：前端 `index.html` 不得引用 Google Fonts、unpkg、cdnjs 等外部资源；所有静态资源必须打包到镜像中
- **CORS 必须支持 `*`**：`CORS_ALLOWED_ORIGINS=*` 时允许所有来源，不能仅做精确匹配
- **Nginx 反代配置**：必须设置 `client_max_body_size`（≥50m）、`proxy_read_timeout`（≥120s）
- **数据库连接双模式**：同时支持 `DATABASE_URL` 整串和 `DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME` 分字段
- **迁移路径可配置**：容器内通过 `MIGRATIONS_PATH` 环境变量指定迁移文件位置

### 离线部署支持

- 离线包应包含前端镜像、后端镜像与 compose/env 模板
- 客户服务器通过 `docker load` 导入镜像，无需访问互联网
- 安装脚本自动检测系统环境、安装 Docker、生成密钥

### 数据管理

- 备份：`docker compose ... exec -T postgres pg_dump ... | gzip > backups/lims-*.sql.gz`
- 恢复：`gunzip -c <backup>.sql.gz | docker compose ... exec -T postgres psql ...`
- Docker Volume 持久化：`pgdata`（核心）、`redisdata`、`caddy_data`
- 建议 crontab 每日自动备份 + 定期外传

## 配置管理

- ConfigMap 管应用配置，Secret 管敏感信息（推荐 External Secrets + Vault）
- Helm values 分环境：values.yaml / values-staging.yaml / values-production.yaml
- 私有化使用 `.env.onprem` 文件（已在 `.gitignore` 中）

## CI/CD（强制）

Lint → Test → Security → Build → Deploy

- Staging 自动部署，Production 手动触发（仅 main）

## 优雅关闭顺序（强制）

1. HTTP Server 停接新请求（30s 超时）
2. Asynq Worker 停拉新任务（gracePeriod=300s，等待进行中的 PDF 生成完成）
3. 关闭 DB 连接池
4. 关闭 Redis

## 数据库高可用（私有化可选）

- **方案**：PostgreSQL 流复制 + repmgr 自动 failover
- **编排**：`deploy/docker/docker-compose.onprem-ha.yml`，bitnami/postgresql-repmgr 镜像
- **读写分离**：备库同时充当只读副本，`DB_REPLICA_DSNS` 配置即生效（GORM dbresolver 已就绪）
- **连接池**：PgBouncer transaction 模式前置

## 文件存储

- 接口：`internal/pkg/storage/storage.go`（`Backend`：Put/Get/Delete）
- 实现：`Local`（本地磁盘）、`S3`（兼容 R2 / MinIO）
- 私有化文件多场景：`docker-compose.onprem.yml` 中 MinIO 服务（`--profile minio` 启用）
- 环境变量切换：`STORAGE_BACKEND=local|s3`
