---
description: LIMS 代码 Review 统一规范
alwaysApply: true
---

# 代码 Review 统一规范

Review 默认只读，不修改现有功能；若用户明确要求修复，先给风险和方案，再做最小范围改动。

## 输出顺序

1. 先列 Bug / 风险，按 P0/P1/P2 排序；每条必须包含文件路径、问题原因、影响范围、建议修复方向。
2. 再列规范性问题与测试缺口；不要把无证据的风格偏好写成 Bug。
3. 最后给验证结果：后端 `go test ./...`，前端 `pnpm lint`、`pnpm type-check`、`pnpm test`，不能执行时说明原因。

## 必查清单

- 前端：请求层统一透传 `X-Trace-ID`、写操作幂等键、401 刷新、权限路由、乐观锁 `updated_at`、Electron IPC 路径校验、私有化禁止硬编码公网域名或 CDN。
- 后端：Handler 参数校验、权限校验、事务边界、审计日志、trace_id、错误封装、幂等键语义、租户/数据范围过滤、Redis 失效、Asynq 异步化。
- 数据库：迁移可回滚、租户列与索引、唯一约束、外键策略、列表查询索引、JSONB/Gin/Trgm 使用、审计分区不过期、更新/删除类主数据使用乐观锁。
- 部署：所有外部地址/密钥走环境变量；私有化不得依赖公网；`CORS_ALLOWED_ORIGINS=*` 必须符合浏览器凭证请求语义。

## 严重级别

- P0：安全越权、数据丢失/串租户、生产启动失败、核心证书生成不可用。
- P1：高概率业务阻塞、性能/并发风险、私有化部署失败、审计或一致性缺口。
- P2：测试缺口、可维护性问题、低概率边界错误或规范偏差。
